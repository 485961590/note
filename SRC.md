 总流程

```text
0 圈范围
  -> 1 信息收集
  -> 2 功能遍历
  -> 3 假设与验证
  -> 4 链式放大或切换方向
  -> 5 无害验证、留证与报告
  -> 6 复盘并更新自己的测试表
```

核心判断：

```text
漏洞 = 开发者假设 - 实际行为
```

打开一个功能时，不要只问“这里有没有漏洞”，而要问：

- 开发者假设了什么？
- 服务端真正校验了什么？
- 哪些信息由用户提供，哪些信息应该由服务端决定？
- 操作前后，权限、数据和状态发生了什么变化？

例如：

- 修改购物车数量：金额是否始终由服务端重新计算？
- 加载个人订单：服务端是否相信了前端传来的用户编号？
- 发送验证码：冷却时间、次数和验证码状态是否由服务端控制？

“假设”不等于“确认”。推断出的风险应标为“待验证”；只有拿到可重复的响应、状态变化或受控观测证据后，才标为“已确认”。

信息收集
目标不是立刻找漏洞，而是建立资产和入口地图。

| 方向 | 关注内容 | 可能得到的线索 |
|---|---|---|
| 子域与资产 | 主站、老系统、测试环境、边缘服务 | 被遗忘的应用或未同步的权限控制 |
| 技术指纹 | 框架、组件、服务、版本 | 后续核对公开漏洞公告的依据 |
| 目录与文件 | API 文档、管理入口、备份文件、配置文件 | 隐藏入口、错误配置、信息泄露 |
| JavaScript | 路由、接口、参数、功能开关、公开配置 | 前端未展示的 API 或业务功能 |
| 历史资产 | 旧域名、旧接口、下线页面 | 仍可访问的旧功能 |

原记录中的常用方式包括：`subfinder`、`crt.sh`、FOFA、Hunter、Quake、Wappalyzer、WhatWeb、`dirsearch`、`ffuf`、浏览器开发者工具、`jsfinder` 和 `waybackurls`。

信息收集阶段的产出应是一张清单，而不是一堆零散结果：

```text
资产 -> 技术栈 -> 功能入口 -> 认证要求 -> 可能涉及的数据或权限
```


准备至少两个自己的测试账号，必要时准备普通用户、项目成员和管理员等不同角色。
像正常用户一样走完整业务流程，并记录请求：

```text
注册 -> 登录 -> 修改资料 -> 找回密码 -> 搜索 -> 评论
-> 上传 -> 下单 -> 优惠券 -> 消息 -> 分享 -> 导入导出
```

阅读 HTTP History 时，给请求打标签：

- 是否带有用户编号、订单编号、文件编号或项目编号？
- 是否带有金额、数量、等级或角色字段？
- 是否接受 URL、回调地址、文件名或路径？
- 是否会读取、创建、修改或删除对象？
- 是否发生了跨角色或跨项目的数据访问？

这些标签就是阶段 3 的测试假设清单。

每个功能记录一行：

```text
功能：
身份：
对象：
输入：
状态变化：
信任边界：
假设：
验证结果：待验证 / 已确认 / 未复现
证据：
影响范围：
```

每个漏洞假设都经过：

```text
现象 -> 假设 -> 最小验证 -> 可重复证据
```

每做完一个练习，强制回答：

- 入口在哪里？
- 信任边界在哪里？
- 服务端错误地相信了什么？
- 我用什么最小证据证明它？
- 现实业务中的影响是什么？

长期要形成的不是漏洞清单，而是看到功能后能迅速判断：

```text
谁在操作？
操作谁的对象？
服务端相信了谁提供的信息？
操作前后状态有什么变化？
还有没有另一个角色或另一个状态？
```

**输入框逻辑缺陷**
- 万能密码
- 用户名与密码错误明确告知（存在爆破风险）
- 超长密码与正确用户名与错误用户名存在明显响应时间差异（用户名枚举）
- 总结就是看差异来确定用户名是否正确

**验证码逻辑缺陷**
- 验证码为绑定手机号
- 短信轰炸（并发与intruder，1分钟15条）
- 手机号验证码双发：
	发送验证码时拦截数据包添加多个手机号参数看多个手机号是否会收到同一个验证码（邮箱同理）
	GET：参数?phone=电话号码1，电话号码2
			?phone=电话号码1&phone=电话号码2
	POST：
		phone=号码1&phone=号码2
		JSON格式
			{
			"phone"="电话号码",
			"phone"="电话号码2",
			}
- 短信内容可修改，例如直接将要发送的内容显示再POST请求体里，但这里应该是直接后端发送走不应该显示的
- 验证码爆破（4位概率高）
- 默认验证码漏洞，开发者为了方便测试留下了默认验证码导致可以直接使用。