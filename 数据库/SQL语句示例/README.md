# SQL 语句示例

统一使用**学生选课管理系统**场景，三张表：`students`、`courses`、`enrollments`。

| 文件 | 内容 |
|------|------|
| [INSERT.md](INSERT.md) | 插入单行/多行、INSERT SELECT、ON DUPLICATE KEY、INSERT IGNORE 等 |
| [SELECT.md](SELECT.md) | 条件查询、排序分页、聚合分组、JOIN、子查询、窗口函数、CASE WHEN、EXISTS 等 |
| [UPDATE.md](UPDATE.md) | 单列/多列更新、表达式更新、多表关联更新、CASE WHEN 更新、事务安全 等 |
| [DELETE.md](DELETE.md) | 条件删除、多表删除、去重删除、软删除、DELETE vs TRUNCATE vs DROP 对比 |
| [SET.md](SET.md) | 系统变量、用户变量、SQL 模式、隔离级别、字符集、安全模式 等 |
| [JOIN.md](JOIN.md) | INNER/LEFT/RIGHT/CROSS/SELF/NATURAL JOIN、非等值连接、USING 语法 等 |
| [事务.md](事务.md) | COMMIT/ROLLBACK、SAVEPOINT、隔离级别、FOR UPDATE、死锁分析 等 |
| [DDL.md](DDL.md) | CREATE TABLE/INDEX/VIEW/存储过程/函数/触发器、ALTER、DROP、约束与外键 等 |

每份文件末尾都有场景练习，建议遮住答案自己先写一遍。
