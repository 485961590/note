```bash
git status  # 查看待提交文件（红色未跟踪/绿色已暂存）  
git add .  # 添加所有修改（或指定文件路径）  
git commit -m "描述性提交信息"  # 如"修复登录页样式问题"  
git push -u origin main  
git pull origin main 拉取远程到本地（Github上修改后本地提交需使用）  
git push  # 简写形式（首次推送后自动关联分支）  
Wjj123456!
```