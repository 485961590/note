# SRC 测试地图

> 适用于已获得书面授权的 SRC 项目、自有资产或专用练习环境。
> 测试以最小影响为原则，不访问、修改或删除真实用户数据。

## 一、总流程

```text
0 圈范围
  -> 1 信息收集
  -> 2 功能遍历
  -> 3 假设与验证
  -> 4 链式放大或切换方向
  -> 5 无害验证、留证与报告
  -> 6 复盘并更新自己的测试表
```

核心判断：

```text
漏洞 = 开发者假设 - 实际行为
```

打开一个功能时，不要只问“这里有没有漏洞”，而要问：

- 开发者假设了什么？
- 服务端真正校验了什么？
- 哪些信息由用户提供，哪些信息应该由服务端决定？
- 操作前后，权限、数据和状态发生了什么变化？

例如：

- 修改购物车数量：金额是否始终由服务端重新计算？
- 加载个人订单：服务端是否相信了前端传来的用户编号？
- 发送验证码：冷却时间、次数和验证码状态是否由服务端控制？

“假设”不等于“确认”。推断出的风险应标为“待验证”；只有拿到可重复的响应、状态变化或受控观测证据后，才标为“已确认”。

## 二、阶段 0：圈定范围

开始测试前，先确认 SRC 规则中的三件事：

| 项目 | 需要确认的内容 |
|---|---|
| 测试范围 | 哪些域名、应用、API、移动端或小程序在范围内 |
| 接收类型 | 哪些漏洞类型收录，哪些类型不收 |
| 禁止行为 | 爆破、批量拉取数据、影响真实业务、删除或修改他人数据等 |

范围不明确时，先向项目方确认。挖到漏洞但违反规则，报告可能无效，也可能造成不必要的影响。

## 三、阶段 1：信息收集

目标不是立刻找漏洞，而是建立资产和入口地图。

| 方向 | 关注内容 | 可能得到的线索 |
|---|---|---|
| 子域与资产 | 主站、老系统、测试环境、边缘服务 | 被遗忘的应用或未同步的权限控制 |
| 技术指纹 | 框架、组件、服务、版本 | 后续核对公开漏洞公告的依据 |
| 目录与文件 | API 文档、管理入口、备份文件、配置文件 | 隐藏入口、错误配置、信息泄露 |
| JavaScript | 路由、接口、参数、功能开关、公开配置 | 前端未展示的 API 或业务功能 |
| 历史资产 | 旧域名、旧接口、下线页面 | 仍可访问的旧功能 |

原记录中的常用方式包括：`subfinder`、`crt.sh`、FOFA、Hunter、Quake、Wappalyzer、WhatWeb、`dirsearch`、`ffuf`、浏览器开发者工具、`jsfinder` 和 `waybackurls`。

信息收集阶段的产出应是一张清单，而不是一堆零散结果：

```text
资产 -> 技术栈 -> 功能入口 -> 认证要求 -> 可能涉及的数据或权限
```

## 四、阶段 2：功能遍历

准备至少两个自己的测试账号，必要时准备普通用户、项目成员和管理员等不同角色。

像正常用户一样走完整业务流程，并记录请求：

```text
注册 -> 登录 -> 修改资料 -> 找回密码 -> 搜索 -> 评论
-> 上传 -> 下单 -> 优惠券 -> 消息 -> 分享 -> 导入导出
```

阅读 HTTP History 时，给请求打标签：

- 是否带有用户编号、订单编号、文件编号或项目编号？
- 是否带有金额、数量、等级或角色字段？
- 是否接受 URL、回调地址、文件名或路径？
- 是否会读取、创建、修改或删除对象？
- 是否发生了跨角色或跨项目的数据访问？

这些标签就是阶段 3 的测试假设清单。

## 五、阶段 3：看到什么，测什么

| 流量或功能特征 | 优先假设 | 下一步 |
|---|---|---|
| `userId`、`orderId`、`docId`、`projectId` | 对象级授权可能不足 | 用两个自己的账号做对照，比较访问和修改结果 |
| `role`、`isAdmin`、`level` | 角色或批量赋值校验可能不足 | 检查服务端是否独立判断权限，不只依赖前端隐藏 |
| `price`、`amount`、`count` | 金额、数量或重复操作逻辑 | 只使用测试订单验证边界值、状态和重复提交 |
| 找回密码流程 | 验证码、步骤或令牌逻辑缺陷 | 检查步骤能否跳过、令牌是否绑定账号和状态 |
| 登录接口 | 账户枚举、会话或验证码问题 | 比较不同失败场景的响应，不进行高频尝试 |
| 搜索、评论、昵称、备注、富文本 | 输入在不同上下文中被错误处理 | 使用唯一的无害标记，观察存储、回显和转义位置 |
| 上传、头像、附件 | 文件类型、访问权限或存储处理不严 | 使用无害测试文件，检查公开访问、下载权限和删除状态 |
| `download`、`export`、`read` | 文件或对象读取权限不足 | 只验证测试目录和测试对象的边界 |
| `url`、`link`、`image`、`callback` | 服务端可能主动访问外部地址 | 只使用自己控制的观测地址确认是否发生服务端访问 |
| XML 请求 | XML 解析器配置风险 | 仅在隔离实验环境中验证，不读取真实主机文件 |
| Swagger、API 文档、GraphQL | 接口鉴权可能不一致 | 整理接口后检查不同角色和未登录状态的权限差异 |
| `redirect`、`next`、`url` | 跳转目标校验不足 | 使用无害外部地址，确认是否可被任意控制 |
| 报错页、调试参数 | 内部路径、版本或服务信息泄露 | 最小化复现并保存响应，不进行大范围探测 |

一次只改变一个变量：账号、对象编号、请求方法、请求体格式或业务状态。这样才能知道究竟是什么因素导致了差异。

## 六、阶段 4：卡住时的转向

| 当前状况 | 转向方式 |
|---|---|
| 主站入口竞争激烈 | 转向范围内的老子域、测试环境、App 或小程序 API |
| 发现低危信息 | 思考它是否能影响登录、权限、数据或后台入口，但只做授权范围内的最小验证 |
| 参数都测过仍无结果 | 回到 HTTP History，重新检查未认真阅读的请求和状态变化 |
| 业务很复杂 | 画流程图，只标记钱、权限、数据分别在哪一步被服务端信任 |
| 某条线被拦截 | 不要持续加大请求量，先换功能、资产或业务假设 |
| 已有一个疑似问题 | 先复现并留证，再决定是否存在合理的影响链 |

链式思维的重点不是“把漏洞打深”，而是判断多个低影响问题是否在授权范围内共同造成更大影响。任何涉及真实数据、真实账户或破坏性操作的验证都应停止扩大，并改用测试对象或向项目方确认。

## 七、阶段 5：无害验证与报告

### 验证原则

- 使用两个自己的测试账号。
- 使用自己创建的对象、订单、文件和项目。
- 需要外部观测时，只使用自己控制的地址。
- 证明存在后立即停止，不批量读取真实数据。
- 对删除、支付、发信、改权限等操作，优先使用测试流程。

### 证据清单

- 完整请求和响应。
- 测试账号及对象的前后状态。
- 必要的截图和时间线。
- 最小复现步骤。
- 实际影响范围及限制条件。

标题应描述影响，而不是只描述现象：

```text
某接口的对象级授权不足，可读取其他测试用户的订单信息
```

不要只写：

```text
某接口存在越权
```

## 八、个人复盘模板

每个功能记录一行：

```text
功能：
身份：
对象：
输入：
状态变化：
信任边界：
假设：
验证结果：待验证 / 已确认 / 未复现
证据：
影响范围：
```

每个漏洞假设都经过：

```text
现象 -> 假设 -> 最小验证 -> 可重复证据
```

不要把“接口返回 200”直接当成漏洞。真正有价值的是权限边界被突破、数据被读取、状态被非法改变，或者影响能够稳定复现。

## 九、从模仿到形成自己的路

1. 先练两个账号的对象级授权测试。
2. 再练登录、找回密码、会话和角色切换。
3. 然后练订单、优惠券、邀请和分享等业务逻辑。
4. 再练输入流转、文件权限和信息泄露。
5. 最后学习服务端请求、反序列化和复杂 API 等高风险方向。

练习环境可以选择 PortSwigger Web Security Academy、OWASP Juice Shop 或 WebGoat。

每做完一个练习，强制回答：

- 入口在哪里？
- 信任边界在哪里？
- 服务端错误地相信了什么？
- 我用什么最小证据证明它？
- 现实业务中的影响是什么？

长期要形成的不是漏洞清单，而是看到功能后能迅速判断：

```text
谁在操作？
操作谁的对象？
服务端相信了谁提供的信息？
操作前后状态有什么变化？
还有没有另一个角色或另一个状态？
```
