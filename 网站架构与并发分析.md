# 网站架构与并发分析

> 以最简单的 LAMP 论坛为例，分析一个网站的组成、并发能力及扩展路径。

---

## 一、一个网站独立运行需要哪些部分？

把一个网站类比成一家餐厅：

```
┌─────────────────────────────────────────────────────┐
│  用户浏览器                                          │
│  (HTML + CSS + JavaScript → 渲染成你看到的页面)       │
│  注：TypeScript 需编译为 JavaScript 才能在浏览器运行   │
└────────────┬────────────────────────────────────────┘
             │  HTTP/HTTPS 请求
             ▼
┌─────────────────────────────────────────────────────┐
│  域名系统 DNS                                        │
│  (把 "example.com" 翻译成服务器 IP 地址)              │
│  注：严格来说非必需，直接用 IP 也能访问，但实际使用     │
│  中 DNS 不可或缺（HTTPS 证书、用户体验等依赖域名）     │
└────────────┬────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────┐
│  Web 服务器 (Apache / Nginx)                         │
│  角色：门口的接待员——接收请求，分发任务                 │
└────────────┬────────────────────────────────────────┘
             │  动态请求（如 /login.php）
             ▼
┌─────────────────────────────────────────────────────┐
│  应用后端 (PHP / Python / Node.js ...)               │
│  角色：后厨——处理业务逻辑，决定"该干什么"              │
└────────────┬────────────────────────────────────────┘
             │  需要读写数据时
             ▼
┌─────────────────────────────────────────────────────┐
│  数据库 (MySQL / PostgreSQL / SQLite)                │
│  角色：仓库——存储用户、帖子、订单等所有持久化数据       │
└─────────────────────────────────────────────────────┘
```

### 总结：六大件 + 一个前提

| 组件 | 干什么的 | 示例 |
|------|---------|------|
| **域名 + DNS** | 让人用名字而非 IP 找到你的服务器（实用层面必需） | example.com |
| **服务器硬件/VPS** | 跑所有软件的那台机器 | 一台 1核2G 的云服务器 |
| **Web 服务器** | 接收 HTTP 请求，返回文件或转发给后端 | Apache |
| **后端程序** | 处理业务逻辑、生成动态页面 | PHP |
| **数据库** | 持久化存储数据 | MySQL |
| **前端文件** | 浏览器里展示的页面、样式、交互 | HTML + CSS + JavaScript |
| **网络带宽**（前提） | 没有它，所有数据都传不出去 | 云服务器通常含固定带宽 |

---

## 二、最简单的网站能同时服务多少人？

这个问题没有标准答案——不同操作的资源消耗差距极大。以下基于 **1 核 CPU / 2GB 内存** 的云服务器给出经验范围：

| 场景 | 大致并发数 | 原因 |
|------|-----------|------|
| **纯静态页面**（HTML/CSS/JS，CDN 或本地） | 几千甚至上万 | Nginx/Apache 对静态文件几乎不消耗 CPU，瓶颈在带宽 |
| **简单动态页面**（PHP 查一次数据库就返回） | 100 ~ 500 | 瓶颈在 PHP 进程数和数据库连接数 |
| **复杂动态页面**（多表联查 + 图片处理 + 复杂计算） | 20 ~ 100 | CPU 和数据库 I/O 严重受限 |
| **论坛帖子列表页**（典型场景） | 200 ~ 500 | 页面逻辑不复杂，但每次都要查数据库 |

### 关键限制因素（按影响力排序）

1. **数据库连接数**
   - MySQL 默认最大连接数 151（5.7+ 版本）。每个请求占用一个连接，连接池用完后后来的请求排队等待。

2. **PHP 进程数（Apache prefork 模式）**
   - Apache 的 `MaxRequestWorkers`（2.2 版本称为 `MaxClients`）通常默认 256，受可用内存限制。
   - 每个 prefork 进程大约占 30~50MB 内存。2GB 内存去掉系统和 MySQL 的占用后，能跑 30~50 个 PHP 进程。
   - 进程耗尽后，新请求在 TCP 层面排队，超时则被丢弃。

3. **CPU**
   - 一个 PHP 请求消耗几毫秒到几百毫秒 CPU 时间。
   - 1 核 CPU 每秒约有 1000ms 可用。若单个请求平均耗时 50ms，理论极限约 **20 请求/秒（QPS）**，即约 1200 个请求/分钟。
   - 注意：这是 **CPU 密集型** 场景的极限，大部分 Web 请求在等 I/O 而非占满 CPU。

4. **磁盘 I/O**
   - 机械硬盘（HDD）随机读写很慢，数据库查询频繁时这是隐形瓶颈。
   - SSD 可将随机读写速度提升几十倍，是性价比最高的硬件升级。

### 一个现实的估算

LAMP 论坛（Apache + PHP + MySQL），1核2G 服务器，不做任何优化：

- **同时在线** 200~300 人通常没有问题。
- "同时在线" ≠ "同时发请求"：真正同时发出 HTTP 请求的用户通常只占在线的 10%~20%。
- 对应的实际吞吐量约 **20~60 QPS**，这在上述各限制因素的安全范围内。

---

## 三、后期如何提升并发数？

提升并发有两条路：**先纵向（单机压榨到极致），再横向（扩展到多台机器）。**

### 第一阶段：单机优化（花时间为主，花钱为辅）

#### 1. 静态文件分离 + CDN

将 HTML/CSS/JS/图片等静态资源放到 CDN 或独立静态服务器上。Web 服务器不再处理静态文件，省下大量连接数和带宽。这是收益最大、成本最低的优化。

#### 2. 页面缓存

把动态页面渲染结果缓存起来：

- **文件缓存**：将生成的 HTML 存为静态文件，直接返回。
- **内存缓存**：用 Redis 或 Memcached 缓存查询结果和页面片段。
- **效果**：100 个人看同一个帖子列表，只需查一次数据库。

#### 3. 数据库优化

- **添加索引**：最常见的性能提升手段。一个正确的索引可以让查询从全表扫描变成索引查找，差距可达几十倍到上千倍。
- **慢查询优化**：开启 `slow_query_log`，逐一优化耗时超过阈值的 SQL。
- **注意**：MySQL 8.0 已彻底移除 Query Cache 功能（5.7.20 开始废弃）。替代方案是在应用层用 Redis 等做查询结果缓存，效果更好且可控。

#### 4. PHP 及运行时优化

- **OPcache**：将 PHP 代码的编译结果（opcode）缓存到内存，避免每次请求都重新解析和编译。对性能提升非常明显。
- **升级 PHP 版本**：PHP 7/8 比 PHP 5 在执行速度上有 2~3 倍的提升。
- **关闭不必要的 Apache 模块**，减少每个进程的内存占用。

#### 5. Web 服务器选型与调优

- **换用 Nginx**：相比 Apache prefork 模式，Nginx 的事件驱动架构在处理大量并发连接时内存占用更低、效率更高。
- **调整连接数和进程数**：根据服务器内存合理配置工作进程数量和每个进程的连接数上限。
- **启用 Gzip/Brotli 压缩**：减少传输数据量，间接降低带宽压力。

#### 6. 升级硬件

最直接但需要花钱的方式：

- 更多 CPU 核心
- 更大的内存（让更多 PHP 进程和数据库缓存到内存中）
- **SSD 替换 HDD**（性价比最高的单次硬件升级）

1核2G + HDD → 4核8G + SSD，轻松承载 **5~10 倍**于原来的并发量。

> 单机优化做好的话，同一台 1核2G 服务器从 200 人提到 **1000~2000 人**在线是完全可能的。

---

### 第二阶段：水平扩展（需要花钱，但有理论上限）

```
                 负载均衡器
          (Nginx / HAProxy / 云厂商 SLB)
                      │
         ┌────────────┼────────────┐
         ▼            ▼            ▼
     服务器1       服务器2       服务器3     ← 多台相同的后端（无状态）
         │            │            │
         └────────────┼────────────┘
                      ▼
            ┌──────────────────┐
            │   数据库层扩展     │
            │   主库（写）       │
            │   从库1  从库2（读）│  ← 读写分离
            └──────────────────┘
                      │
                      ▼
            Redis 缓存集群         ← 共享缓存和 Session
                      │
                      ▼
              消息队列 (RabbitMQ / Kafka)
              ← 异步处理耗时任务
```

#### 核心思路

1. **无状态化**
   - 每台后端服务器不保存任何用户状态（Session 存入共享 Redis）。
   - 任意一台宕机不影响服务，负载均衡器自动剔除故障节点。

2. **读写分离**
   - 数据库主库负责写入，多个从库负责读取。
   - 大多数 Web 应用读写比例在 10:1 以上，从库可以线性扩展读能力。

3. **缓存层独立**
   - Redis 集群独立部署，所有服务器共享同一缓存。
   - 避免各服务器各自缓存导致的数据不一致。

4. **消息队列异步化**
   - 耗时操作（发邮件、生成报表、图片处理）从 Web 请求中剥离，放入消息队列异步处理。
   - Web 请求只负责快速响应，不阻塞等待耗时任务。

---

### 不同阶段的参考量级

| 阶段 | 架构 | 大致承载量（同时在线） |
|------|------|----------------------|
| 起步 | 单台 LAMP + 基本优化 | 几百 ~ 一千 |
| 成长 | + CDN + Redis + 数据库索引优化 | 几千 ~ 一万 |
| 扩张 | + 负载均衡 + 多台后端 + 读写分离 | 几万 ~ 十几万 |
| 大型 | + 分库分表 + 微服务 + 多机房 | 百万级 |

---

## 一句话总结

一个网站跑起来最精简只需要 **域名（实用层面）、服务器、Web 服务器软件、后端语言、数据库、前端文件** 六样东西，外加网络带宽这个前提条件。用 LAMP 栈（Linux + Apache + MySQL + PHP），1核2G 最低配云服务器可以轻松扛住 **200~500 人同时在线**（实际并发请求约 20~60 QPS）。

提升并发的方法是：

> **先单机压榨**（缓存 + 索引 + 配置优化 + 硬件升级）→ **再多机分摊**（负载均衡 + 读写分离）

这两步走完，从几百人到几十万人的增长路径是清晰的。
